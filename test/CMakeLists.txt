file(GLOB sources "*.hpp" "*.cpp" "../silicium/*.hpp")
if(LUA51_FOUND)
	file(GLOB luaSources "lua/*.cpp")
endif()
add_executable(test ${sources} ${luaSources})
target_link_libraries(test silicium ${LUA_LIBRARIES} ${ZLIB_LIBRARY} ${URIPARSER_LIBRARIES})
if(NOT MSVC)
	target_link_libraries(test ${Boost_LIBRARIES})
endif()
if(SILICIUM_LINUX)
	target_link_libraries(test dl)
endif()
