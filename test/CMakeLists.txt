file(GLOB sources "*.hpp" "*.cpp" "observable/*" "source/*" "sink/*")
file(GLOB_RECURSE headers "../silicium/*.hpp")
if(LUA51_FOUND)
	file(GLOB luaSources "lua/*.cpp")
endif()
if(ZLIB_FOUND)
	file(GLOB zlibSources "zlib/*.cpp")
endif()
if(URIPARSER_FOUND)
	file(GLOB uriSources "uri/*.cpp")
endif()
add_executable(unit_test ${sources} ${luaSources} ${zlibSources} ${uriSources} ${headers})
target_link_libraries(unit_test siliciumc ${ZLIB_LIBRARY} ${URIPARSER_LIBRARIES})
if(NOT MSVC)
	target_link_libraries(unit_test ${Boost_LIBRARIES})
endif()
if(SILICIUM_LINUX)
	target_link_libraries(unit_test dl)
endif()
