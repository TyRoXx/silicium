file(GLOB sources "*.hpp" "*.cpp" "../silicium/*.hpp" "../reactive/*.hpp")
if(LUA51_FOUND)
	file(GLOB luaSources "lua/*.cpp")
endif()
add_executable(test ${sources} ${luaSources})
target_link_libraries(test silicium reactive ${Boost_LIBRARIES} ${LUA_LIBRARIES})
